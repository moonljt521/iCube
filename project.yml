name: iCube

options:
  bundleIdPrefix: com.moonding
  createIntermediateGroups: true
  deploymentTarget:
    iOS: "18.0"

settings:
  base:
    SWIFT_VERSION: "5.0"
    TARGETED_DEVICE_FAMILY: "1"
    ENABLE_USER_SCRIPT_SANDBOXING: NO
    SWIFT_EMIT_LOC_STRINGS: YES
    IPHONEOS_DEPLOYMENT_TARGET: "18.0"
    DEVELOPMENT_TEAM: 75L84BV98K
    CODE_SIGN_STYLE: Automatic

packages:
  CubeKit:
    path: CubeKit
  CubeSolve:
    path: CubeSolve
  CubeScan:
    path: CubeScan

targets:
  iCube:
    type: application
    platform: iOS
    sources:
      - path: iCube
        excludes:
          - "Info.plist"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.moonding.icube
        PRODUCT_NAME: iCube
        INFOPLIST_FILE: iCube/Info.plist
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: "1.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        SUPPORTS_MACCATALYST: NO
        # 顶层 settings.base 里那份会被 xcodegen 对 iOS target 注入的默认值
        # "1,2" 覆盖掉（target 级优先于项目级），所以这里必须再声明一次。
        # 只声明竖屏又不带 UIRequiresFullScreen 时，多出来的 iPad 支持会触发
        # "All interface orientations must be supported" 警告。
        TARGETED_DEVICE_FAMILY: "1"
    dependencies:
      - package: CubeKit
      - package: CubeSolve
      - package: CubeScan

  iCubeTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: iCubeTests
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.moonding.icube.tests
        GENERATE_INFOPLIST_FILE: YES
        TARGETED_DEVICE_FAMILY: "1"
    dependencies:
      - target: iCube
      - package: CubeKit
      - package: CubeSolve
      - package: CubeScan
